name: Windows Check
on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
jobs:
  win-check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Windows Check Passed"
