[project]
name = "swim-meet-setup"
version = "1.0.0"
description = "Swim Meet Setup Tools"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.61.0",
    "pytest>=9.1.1",
    "pytest-playwright>=0.8.0",
]
